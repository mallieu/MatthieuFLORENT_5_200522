const variablesGlobales = {
    // Recherche de l'ID dans l'URL
    urlParams : new URLSearchParams(window.location.search),
    // URL de l'API
    urlAPI : "http://localhost:3000/api/products"
}

export { variablesGlobales };