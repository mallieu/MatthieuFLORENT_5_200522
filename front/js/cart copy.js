const localStoragePanier = localStorage


const addToCart = document.getElementById("addToCart");    
addToCart.addEventListener('click', function() {       
    // Transformation de l'objet pour le mettre dans le panier
    creationProduit()
    // Ajout au panier
        // SI déjà existant, ajouter quantité seulement   
        // push.produit dans listeProduit            
});



function creationProduit(quantity, id, color) {
    this.quantity = quantity,
    this.id = id,
    this.color = color
    if (this.id == id AND this.color = color) {

    };

};




let produitActif = 